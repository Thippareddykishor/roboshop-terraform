configs:
  params:
    server.insecure: true

#for nginx ingress
server:
  ingress:
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt
    enabled: true
    ingressClassName: nginx
    tls: true
    # extraT